"""Documented prompt templates (Research_Design_v3 §6 anti-pattern compliance).

Each file in this directory holds ONE template + its assembly helper. The
template text is a module-level constant so it can be cited verbatim in the
writeup (no silent engineering).
"""
